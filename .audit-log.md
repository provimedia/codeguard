# Code Guardian Audit Log

## 2026-07-08 — v13 Generalization Gate (Anti-Beispiel-Hardcoding)

- **Finding (design):** Der Marker-Kommentar selbst zählt als intentional, wenn er
  ein suspicious literal + "CASE:" enthält (DECISION_RE matcht das Wort CASE im
  Markernamen) — harmlos (nie ein FINDING), dokumentiert statt wegoptimiert;
  Tests erwarten intentional>=1 statt ==1.
- **Selbstanwendung:** das neue Tool lief auf seinen eigenen Diff
  (`--git` → findings=0) und auf die Fixture (9/9 Traps, clean=0). Lesson
  bestätigt: Pfad-Ignores müssen RELATIV zum Scan-Root greifen, sonst macht
  ein test/-Segment im Repo-Pfad die ganze Fixture unsichtbar.
- **Audit verdict:** VERIFIED — pytest-Suite 48 passed (17 neue), Standalone
  17 OK, Fixture-Oracle 9/9+0FP, bash -n exit 0, Wegwerf-HOME ohne
  Missing-markers, symbol-loss findings=0, Selbst-Scan findings=0.

## 2026-07-08 — v12 Team-Paket: Hooks auto-installiert + fail-open jq-Guard

- **Finding (BUG, HIGH, gefixt vor Ship):** decision-gate-check.sh war ohne
  installiertes jq FAIL-CLOSED — leere jq-Ausgabe → grep-Miss → deny für JEDE
  AskUserQuestion, dauerhaft. Auf Mitarbeiter-Maschinen ohne jq hätte das alle
  Optionsfragen blockiert. Fix: `command -v jq || exit 0` (fail-open) in Repo-
  UND lokaler Kopie. Lesson: jeder Hook, der bei Fehler eines Hilfstools einen
  deny-Pfad erreichen kann, braucht einen expliziten Fail-Open-Guard.
- **Finding (process):** v10-Paket verlangte manuellen settings.json-Hand-Merge
  durch jeden Mitarbeiter — fehleranfällig und nicht verifizierbar. install.sh
  macht das jetzt selbst (idempotenter python3-Merge, Backup, korrupte Datei
  wird nie angefasst). hooks/ lebt jetzt im Repo — eine Quelle der Wahrheit,
  Zip = Repo-Snapshot statt Sonderbau.
- **Audit verdict:** VERIFIED — bash -n exit 0; Testmatrix: frische HOME (3
  Einträge, valides JSON) · Idempotenz (2. Lauf, weiterhin 3) · Fremde Keys +
  vorhandener Alt-Eintrag (intakt, nicht gedoppelt, Backup angelegt) · korrupte
  settings.json (unangetastet, warn + manuelle Anleitung) · jq-Guard fail-open
  (EXIT=0, kein Output) · Zip-E2E: Installation direkt aus dem Paket vollständig grün.

## 2026-07-08 — v12 DECISION GATE (recommend-never-decide + PreToolUse enforcement)

- **Finding (docs, HIGH-value):** PreToolUse plain stdout is NOT visible to the model —
  a "soft reminder" hook on AskUserQuestion would have been dead code. Only a deny's
  `permissionDecisionReason` is fed back. Lesson: verify hook-event visibility in the
  official docs BEFORE designing any hook-based reflex; stdout visibility differs per event.
- **Finding (process):** README version line was stale at "v7.1 (Council Edition)" two
  releases long — the version-bump checklist in design-rationale named SKILL.md + install.sh
  but not README's version line. Fixed with v12; checklist now implicitly covers it via this entry.
- **Audit verdict:** VERIFIED — R1 secrets `findings=0`, symbol-loss `findings=0 exit=0`,
  hook unit-matrix 4/4 (deny/allow-de/allow-en/escape), throwaway-HOME install ohne
  Missing-markers, settings.json merge JSON-valide mit intakten Bestands-Hooks.

## 2026-07-08 — Vendor llm-council + installer companion-install

- **Finding (process, MEDIUM):** the skill referenced a companion skill (`llm-council`)
  that was not part of the shipped artifact — three Council Gates silently degraded on
  every machine except the author's. Lesson: every skill a SKILL.md *invokes by name*
  must either ship in the repo or be a hard install-time check, never a silent warn.
- **Finding (upstream, LOW, reported-not-fixed):** vendored `llm-council/SKILL.md`
  contradicts itself on HTML report output (step 5 vs closing notes). Vendored verbatim
  by design; fix upstream.
- **Audit verdict:** VERIFIED — R1 secrets `findings=0`, symbol-loss `findings=0 exit=0`,
  installer 4-path matrix green in throwaway HOME (fresh / preserve / --force / --dry-run).

## 2026-07-08 v14 DEPLOY GATE (tool+reference+SKILL+hook+installer+docs) — APPROVED
C:0 W:2 | Symbol-loss findings=0 · diff-slop findings=0 · hardcoded --git findings=0 exit=0 · R1 findings=0 · R4 findings=1 (api.partner.example.com 4× — pre-existing intentional test/ fixture, report-only) · R2 findings=3 (subprocess boilerplate across pre-existing tools, report-only) · standalone suite 16+17+30+1 green · fixture --root SUMMARY deploy=5 server-only=3 never=10 review=2 exit=1 · hook matrix 6 deny / 5 allow + 4 freshness correct · install.sh --dry-run clean
Self-Slop: removed own unused _segment_match draft (semantic sweep caught what --diff-slop missed — referenced nowhere, first-draft leftover).
PATTERN: lstrip("./") eats the leading dot of dotfiles ('.env'→'env') — use explicit './'-prefix loop; hit once before in path-normalization code.
ADDENDUM v14: live FP found+fixed post-install — hook filename deploy-gate-check.sh matched its own deploy*.sh regex and denied the functional self-test; self-reference carve-out added, matrix re-verified (5 deny / 4 allow incl. self-test cases).
ADDENDUM v14 E2E (post-restart): full harness loop verified — deny without report / dry-run carve-out / allow with fresh APPROVED report / DNS-fail proof. Live bug found+fixed: macOS openrsync emits 9-char itemize prefixes (>f.......), regex expected rsync 3.x's 11 — dir-segment traps (tests/, .env, phpunit.xml) passed as DEPLOY. Regex now {7,9}; regression test added; suite 17 green.
PATTERN: parse formats vary by tool VERSION/IMPLEMENTATION — validate parsers against the locally installed binary's real output, not only the documented format.

## 2026-07-18 v15 DATA GATE (reference+SKILL+debug-wiring+installer+docs+fixture) — VERIFIED
C:0 W:0 | symbol-loss findings=0 exit=0 · hardcoded --git findings=0 exit=0 · tool suite 65/65 green (project AND live install) · install.sh live run: "v15 markers + ... + data gates detected" · diff -rq project↔live clean after install (two-way drift design-rationale/openrsync-test reconciled) · TDD: baseline 6/6 correct at fixture scale (scenario A bug-shaped, scenario B analysis-shaped with authority-supplied wrong criterion) — founding RED evidence is the real production deadline_ab misdiagnosis; GREEN 3/3 with convergent Full-Picture blocks, verdicts MIXED (101-103 INTENDED STATE / 120 DEFECT) correct in all runs.
PATTERN: a data-state verdict formed from known/handed-down columns is blind to unknown-unknown columns BY CONSTRUCTION — countermeasure is a structural output contract (mandatory Full-Picture block), not a prohibition list; strong models already comply substantively, the block makes diligence checkable.
PATTERN: fixture-scale pressure tests cannot reproduce production-scale context-pressure failures with strong models — document the real incident as RED evidence and test the CONTRACT SHAPE (block present + convergent) instead.

## 2026-07-18 — v16 Senior Dev Companion (senior-dev/, hooks v16, install.sh, anchors) — VERIFIED

- Layers run: Logic, Security, Redundancy R1–R5, Self-Slop (diff), Generalization. DB/Efficiency N/A (no DB, per-event hooks).
- Verified-by: test/hooks/run.sh → HOOK SUITES: ALL PASS (2 suites); /usr/local/bin/pytest tools/tests → 65 passed; bash -n all touched shell → SYNTAX-OK; detect-hardcoded-cases.py --git → findings=0; detect-symbol-loss.py → N/A (shell/markdown outside scanner language scope, 0 files scanned — honest gap, covered by bash tests instead).
- FIXED (own-change hardening): SESSION_ID sanitized (tr -cd 'A-Za-z0-9._-' + empty fallback) before use in counter-file path — untrusted-shaped input must not form filesystem paths raw, even from the harness.
- PRE-EXISTING (report-only, Principle 3): ${BASENAME} interpolated into hook JSON since v10 — a filename containing '"' or '\' emits invalid JSON. Recommend jq -Rn escaping in a future hook pass. Not fixed here (predates this change).
- INTENTIONAL: §D heartbeat questions duplicate senior-card §D text as fixed data in the hook — hook must not depend on skill install path at inject time; the card remains the canonical text.
- PLAN-DEFECT LESSON: a plan step that anchors an edit by line number can land inside a fenced template block — the executing subagent followed it verbatim and flagged it (correct behavior). Anchor plan edits by unique PROSE context, never by line number into files containing templates.

## 2026-07-19 — v16 Monitoring-Validierung (cg-testbed, 8 Echt-Sessions) — VERIFIED
- Matrix: BUILD (S1: Intake normal, Journal, 7 Heartbeats, §E) · DEBUG (S2: 5 Phasen, Root-Cause OR→AND, Konsumenten) · PLAN (S3/S3b: §B-DB-Frage Spalte-vs-Tabelle, P1-Whitelist-Falle gefunden, Rückfrage mit Empfehlung) · UPDATE (S4: Blast-Radius 7 Dateien, idempotente Datenmigration) · DELETE (S5: Spike für SQLite-Drop-Reihenfolge, Datenerhalt bewiesen) · RECURRENCE (S6a-c).
- GAP GEFUNDEN+GEFIXT: Sessions vergaben Instanz-Tags (stats-endpoint, csv-export) statt Typ-Tags → Rule of Three konnte nie feuern; zusätzlich doppelte Journal-Zeile pro Task. Fix: Tag-Gesetz (Typ, nie Instanz; bestehende Tags erst lesen; Zählung nach TYP auch über alte engere Tags) + Ein-Zeilen-Gesetz. Re-Test S6c: Vorschlag feuerte korrekt beim 3. gleichartigen Task, inkl. ehrlicher Gegen-Empfehlung und skill-declined-Marker.
- LESSON: Freiform-Klassifikation durch das Modell braucht ein explizites Granularitäts-Gesetz im Skill-Text — "STABLE tags" allein reicht nicht, das Modell wählt sonst die spezifischste Bezeichnung.

## 2026-07-19 — v16.1 Selbstaudit (senior-dev dogfooded auf sich selbst) — APPROVED (2 fixed, 1 declined)
- Gefixt: UPDATE-ANLEITUNG-Titel hing auf v16 (sed erfasste nur Fußzeile/Zip-Name — LESSON: Versions-Bumps per Checkliste ALLER Vorkommen, nicht per Einzel-sed); Spec ohne Tag-Gesetz/Airlock → Addendum ergänzt; Zip mit veralteter Anleitung → neu gebaut.
- Declined (YAGNI, §B "brauchen wir das JETZT?"): Installer-Versions-Grep für senior-dev — der Block überschreibt immer (rm -rf+cp), ein Versions-Marker müsste bei jedem Point-Release gepflegt werden; Nutzen < Wartungskosten.
- Verifiziert: 4 Heartbeat-Fragen ↔ exakt 4 §D-Bullets (1:1), Hook-Text == Triage-Klassen, 6 Anker vorhanden, Suiten grün (Hooks ALL PASS, 65 pytest), Zip-Inhalt nach Rebuild korrekt.

## 2026-07-19 — v16.2 Cockpit: 7 Monitoring-Runden (Ziel: 5 + Verbesserung je Runde) — APPROVED
- R1 DEBUG: Karte ✓, Fix ✓ — Improvisation → T6-Template standardisiert. R2 Trivial: Proportionalität ✓ — Gesetz-10-Widerspruch präzisiert (Trivial = nur Verdict-Block). R3 Frage: null Protokoll ✓. R4 Risky: Karte "groß" + read-only + fehlenden done_at erkannt ✓. R5 Build: Tag-Wiederverwendung ✓, Verdict-Drift → Gesetz 12 (Template-Treue). R6 Build: Rule-of-Three beim 3. Vorkommen ✓, Header-Drift → bindende Abschluss-Regel INLINE in senior-dev §E. R7 Trivial: SAUBER — exakter Header + bindende VERDICT-Zeile + TDD + Playwright-Beweis.
- KERN-LESSON (systemisch): Inline-Regeln im immer geladenen SKILL.md werden zu 100% befolgt; Pointer auf Referenz-Dateien driften. Bindende Formate gehören inline an den Ort des Handelns; Referenzen tragen nur Detail-Templates.
- TOOLING-LESSON: `grep -c` mit 0 Treffern beendet &&-Ketten (Exit 1) — ein Commit ging dadurch still verloren (recommitted). In Verifikations-Ketten `grep -c || true` oder Ketten trennen.
